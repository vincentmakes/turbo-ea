/**
 * DrawIO PreConfig.js — loaded BEFORE app.min.js.
 * Only configuration variables should go here (Draw.loadPlugin is not
 * yet available).  Plugin / runtime code belongs in PostConfig.js.
 */
