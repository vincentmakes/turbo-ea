{
  "name": "turboea-screenshots",
  "private": true,
  "type": "module",
  "scripts": {
    "capture": "npx tsx capture.ts",
    "capture:en": "npx tsx capture.ts --locale en",
    "capture:es": "npx tsx capture.ts --locale es",
    "capture:marketing": "npx tsx capture.ts --marketing",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
